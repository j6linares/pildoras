package util;

import java.util.logging.Logger;

public class Bitacora {
	
	public static final Logger LOG = Logger.getLogger(Bitacora.class.getName());
	private Bitacora() {
	    throw new IllegalStateException("Utility class");
	  }
}
